name: Trading Detector\n\non:  \n  workflow_dispatch:\n  schedule:\n    - cron: '0-59 9-15 * * 1-5'  # Runs during IST trading times (9 AM to 3 PM)\n\njobs:\n  run-detector:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v2\n      - name: Run Detector\n        run: |\n          # Add commands to run the detector here\n          echo 'Running the detector...'\n      - name: Create Issue\n        if: success()\n        run: |\n          # Add commands to create an issue with fund alerts here\n          echo 'Creating issue for fund alerts...'\n